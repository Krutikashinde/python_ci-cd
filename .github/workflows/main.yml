name: Python CI + Docker Pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t my-python-app .

      - name: Run Docker Container
        run: docker run my-python-app